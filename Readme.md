### Setup

Set up a Python 3 virtualenv and install the dependencies on it:

```bash
cd backend
python -m venv env
source env/bin/activate
pip install -r requirements.txt
```

run flask app:

```bash
flask run
```

the backend will run at http://localhost:5000/ .
<br/>
<br/>


#### Frontend Setup

Install the dependencies:

```bash
cd ui
npm install
```

Run the Vue.js frontend:

```bash
npm run dev
```

Voila! you can find the frontend at http://localhost:5173/

<br/>



- Due to short time limit and since i am working full time i have not written types for all the components as well as didnt give too much features but all the required functionality should be working and as per the requirements shared.
